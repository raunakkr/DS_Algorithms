#include<stdio.h>
#include<limits.h>

int a[]={1,3,1,1,2,4,1};
int n;


//:: Recursive implementation for finding min. jump needed to reach an end

	int min_jum_needed(int i){
	int mint=INT_MAX;
	if(i>=n-1)
	return 0;

	if(i==0)
	return INT_MAX;

  for(j=
	mint=min(



	}

